package com.valetgest.enums;

/**
 * Tipos de usuário do sistema.
 * - DONO: tem acesso total, vê todas as unidades
 * - GERENTE: vê apenas a unidade que ele gerencia
 */
public enum TipoUsuario {
    DONO,
    GERENTE
}
